[project]
name = "InversionsResolver"
version = "0.1.0"

dependencies = [
    "pandas",
    "numpy",
    "networkx",
    "matplotlib",
    "openpyxl"
]

[project.scripts]
InversionsResolver = "InversionsResolver.cli:main"