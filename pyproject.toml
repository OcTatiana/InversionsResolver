[project]
name = "InversionResolver"
version = "0.1.0"

dependencies = [
    "pandas",
    "numpy",
    "networkx",
    "matplotlib",
    "openpyxl"
]

[project.scripts]
InversionResolver = "InversionResolver.cli:main"